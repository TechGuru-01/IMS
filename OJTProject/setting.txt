CREATE TABLE IF NOT EXISTS settings (
    setting_key VARCHAR(50) PRIMARY KEY,
    setting_value DECIMAL(10, 4) NOT NULL
);

-- Mag-insert ng default values para hindi error sa simula
INSERT IGNORE INTO settings (setting_key, setting_value) VALUES ('dollar_rate', 57.74), ('yen_rate', 0.37);